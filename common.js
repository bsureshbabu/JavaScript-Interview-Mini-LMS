// common.js - minimal scripts used by pages
document.addEventListener('DOMContentLoaded', () => {
  // future: implement site-wide search, persist progress, etc.
  // For now keep it minimal.
  console.log('Mini LMS loaded');
});
